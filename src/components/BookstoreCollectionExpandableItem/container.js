/*
import {connect} from 'react-redux'
import BookstoreCollectionExpandableItem from './index'
import FSA, * as ActionTypes from './actions'

export const actiions = {
    buyBook: (payload) => FSA(ActionTypes.BUY_BOOK, payload),
    deleteBook: payload => FSA(ActionTypes.DELETE_BOOK, payload)
}

export default connect (actions) (BookstoreCollectionExpandableItem)*/
